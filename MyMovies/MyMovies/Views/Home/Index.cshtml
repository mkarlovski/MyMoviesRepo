@*@{ 
    var firstNum = 2;
    var secondNum = 3;
    var sum = firstNum + secondNum;
}*@

@*@model MyMovies.Models.Movie;*@

@model List<MyMovies.Models.Movie>;   

<!DOCTYPE html>
<html>
<head>
    <title>My Movies</title>
    <link href="~/lib/bootstrap/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    @*<h1>My Movies</h1>
    <a href="./Home/AboutUs">About Us</a>
    <p>The time right now is : @ViewBag.CurrentDate </p>
    <p>The sum of @ViewBag.FirstNumber and @ViewBag.SecondNumber is @ViewBag.Sum</p>*@

    <div class="container" style="margin-top:20px">
        <div class="row">

            @foreach(var movie in @Model)
            {
            <div class="col-md-4">
                <div class="card" style="width: 18rem;">
                    <img src="@movie.ImageURL" style="height:250px" class="card-img-top" alt="...">
                    <div class="card-body">
                        <h5 class="card-title">@movie.Title</h5>
                        <p class="card-text">@movie.Description</p>
                        <a href="#" class="btn btn-primary">Go somewhere</a>
                    </div>
                </div>
            </div>
            }
            


        </div>       
    </div>


</body>
</html>
     